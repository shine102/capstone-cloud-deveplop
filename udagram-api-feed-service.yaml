apiVersion: v1
kind: Service
metadata:
  name: udagram-api-feed
  labels:
    app: udagram-api-feed
spec:
  ports:
  - name: "8100"
    port: 8100
    targetPort: 8100
  selector:
    app: udagram-api-feed